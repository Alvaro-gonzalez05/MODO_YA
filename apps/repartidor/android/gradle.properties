org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# El pub cache esta en D: y el proyecto en C:; con compilacion incremental
# Kotlin falla con "Could not close incremental caches" (distinta raiz).
kotlin.incremental=false
